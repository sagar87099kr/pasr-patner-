// Centralized configuration for the partner portal

// The base URL for the main backend API
// export const BACKEND_URL = process.env.NEXT_PUBLIC_BACKEND_URL || process.env.BACKEND_URL || 'http://localhost:8080';
export const BACKEND_URL = process.env.NEXT_PUBLIC_BACKEND_URL || process.env.BACKEND_URL || 'https://www.pasr.in';
export const BACKEND_BASE_URL = BACKEND_URL;